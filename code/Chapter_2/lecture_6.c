#include <stdio.h>


int main(void) {
	int x, y, result;
	printf("Enter value 1: ");
	scanf("%d", &x);
	printf("Enter value 2: ");
	scanf("%d", &y);

	
	printf("%d is the largest of %d and %d\n", result, x, y);
	return 0;
}
